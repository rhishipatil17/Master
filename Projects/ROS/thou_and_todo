Device
  - create all groups and users with no login and proper permissions
  - startup process should be started automatically

Processes
  - setid() to detach child from parent
  - restrict commads such as 'kill' even within the group

Logging
  - Timestamp
  - PID, TID
  - File name, line number
  - Move lib to foundation
  - Deadlock monitoring is required?

IPC
  - mq_unlink: receiver, after one process completes it's execution, send a message to the other process so that when the other one finishes its own execution, it can call unlink to cleanup the queue. Get mq attributes to check if other process is still reading from the queue.
  - Have a process/thread for entire system cleanup.
  - Message format while sending a message; possible options: TLV
  - encryption of IPC messages
  - priority scheduling
  - Move lib to foundation
  - naming and grouping of message queues
  - timeout for receiving messages

HW
  - have a separate path for all hardware libs

GUI
  - If SPI display used, implement lib for GUI
  - If HDMI display used, use prebuilt lib

COM
  - ioctl to manage Wifi
